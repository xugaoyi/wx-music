<view class="modal" hidden="{{!modalShow}}">
  <view class="bg" bind:tap="onClose"></view>
  <view class="panel" style="bottom:{{footerBottom}}px">
    <!-- <i class="iconfont icon-shanchu1" bind:tap="onClose" /> -->

    <!-- slot插槽 -->
    <slot name="modal-content"></slot>
  </view>
</view>